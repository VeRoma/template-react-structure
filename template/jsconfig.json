{
	"compilerOptions": {
		"baseUrl": "src", // Корневая директория проекта
		"paths": {
			"@/components/*": ["components/*"] // Настройка алиасов для путей
		}
	},
	"include": ["src/**/*"] // Включаем все файлы в папке src
}
